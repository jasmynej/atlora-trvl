DATABASE_URL=

# Cloudflare R2 (S3-compatible) — used by the storage router for media uploads.
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_URL=

# Clerk — server-side secret key, used to verify session JWTs in
# agencyProcedure / platformProcedure. Same Clerk application as
# apps/admin's VITE_CLERK_PUBLISHABLE_KEY.
CLERK_SECRET_KEY=
