# Clerk publishable key — shared by both entry points (index.html and
# platform.html both read the same env at build time). Get this from the
# Clerk dashboard for this project's application.
VITE_CLERK_PUBLISHABLE_KEY=

# Defaults to http://localhost:3001/trpc if unset.
# VITE_API_URL=http://localhost:3001/trpc
