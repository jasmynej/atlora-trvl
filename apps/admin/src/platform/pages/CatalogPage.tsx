import { Card, EmptyState } from '@atlora/ui'

export function CatalogPage() {
  return (
    <Card>
      <EmptyState title="Destination catalog" description="Ships in Milestone 2 — list view, hierarchy, editor, region management." />
    </Card>
  )
}
