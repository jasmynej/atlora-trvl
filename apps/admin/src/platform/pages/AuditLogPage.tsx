import { Card, EmptyState } from '@atlora/ui'

export function AuditLogPage() {
  return (
    <Card>
      <EmptyState title="Audit log" description="Ships in Milestone 5 — filter by actor, action, subject, and date range." />
    </Card>
  )
}
