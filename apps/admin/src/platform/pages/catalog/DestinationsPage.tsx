import { Card, EmptyState } from '@atlora/ui'

export function DestinationsPage() {
  return (
    <Card>
      <EmptyState
        title="Destinations"
        description="List view, hierarchy, editor, and publish state — the first real Milestone 2 surface."
      />
    </Card>
  )
}
