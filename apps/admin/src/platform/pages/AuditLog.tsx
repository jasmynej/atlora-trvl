export default function AuditLog() {
  return (
    <div>
      <h1 className="type-h2 mb-2">Audit Log</h1>
      <p className="type-body-sm text-fg2">
        The audit log viewer ships in Milestone 5. audit_logs rows are being written
        starting this milestone — there's just no viewer for them yet.
      </p>
    </div>
  )
}
